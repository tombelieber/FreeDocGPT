# Ollama Models
EMBED_MODEL=embeddinggemma:300m
GEN_MODEL=gpt-oss:20b

# Database Configuration
DB_DIR=.lancedb
TABLE_NAME=docs

# Ollama Server (if not using default)
# OLLAMA_HOST=http://localhost:11434

# App Configuration
# CHUNK_SIZE=1200
# CHUNK_OVERLAP=200
# TOP_K_RESULTS=5

# Prompt Configuration
# Path to the system prompt file used in chat (absolute or relative to repo root)
SYSTEM_PROMPT_PATH=rag_prompt.md
